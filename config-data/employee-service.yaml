spring:
  config:
    activate:
      on-profile: default
eureka: 
    instance:
      instance-id: ${spring.application.name}:${random.uuid}

--- 
spring:
  config:
    activate:
      on-profile: docker     
server:
  port: 8001
eureka:
  client:
    serviceUrl:
      defaultZone: ${EUREKA_SERVICE_URI:http://eureka-service.microservices.svc.cluster.local:8761/eureka}

